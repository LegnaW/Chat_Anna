# AnnaBot 配置示例
# OneBot 11 协议实现配置

# 机器人 QQ 号
self_id: 10001

# 协议版本
protocol_version: "11"

# WebSocket 服务配置
websocket:
  host: "0.0.0.0"
  port: 8000

# 心跳配置
heartbeat:
  enabled: true
  interval: 5  # 秒

# API 限速
rate_limit:
  enabled: false
  interval: 500  # 毫秒
